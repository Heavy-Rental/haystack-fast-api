# Dynamic Pricing Feature — Execution Plan

> **This file tracks tasks and schedule only.** Decisions and rationale live in
> `dynamic-pricing-masterplan.md` — don't duplicate reasoning here; link back to
> the relevant masterplan section instead.

Last updated: 2026-08-11

Timeline: 5 days total. Day 1–3 = Phase 1 (offline experimentation). Day 4–5 = Phase 2 (productionize).

---

## Day-by-day schedule

**Day 1 — Phase 1a: synthetic data generation**
- Scaffold `ml-experiments/` (sibling to `app/`, per masterplan phase order)
- Write full `generate_synthetic_data.py`: base rates (Pollisum/Ben's Rental rate cards), non-linear duration discounting, monsoon seasonality, utilization bands by category, `distance_km` delivery-distance premium (sampled, not geocoded — see masterplan), References docstring
- Sanity-check generated data distributions (not just "it ran")

**Day 2–3 — Phase 1b: feature engineering, training & SHAP review** ✅ done
- Feature engineering: one-hot `AssetCategory.name`, ordinal `condition` (`NEEDS_REPAIR=0…EXCELLENT=3`), `duration_days`, `capacity`, `distance_km`, `platform_height` (added after baseline SHAP review — see masterplan)
- Train baseline XGBoost model
- SHAP review — confirm duration↓ price and condition (worse → lower price) behave correctly, holding the other fixed
- Finalize `feature_schema.py` based on SHAP findings
- Retrain final model, save artifacts (`.pkl` + `current.json`)

**Day 3 (extension) — Phase 1c: prototype `predict_price()` (ml-experiments)**
- New file `ml-experiments/predict_price.py`: in-process `predict_price(...)` function, loads `model.pkl` once, builds features via the existing `feature_schema.build_features()` — no changes to the locked Phase 1b schema
- Guardrail clamping using static per-category `pricing_tables.CATEGORY_BASE_RATE` bounds — a stand-in, since this prototype has no DB/Asset access; Phase 2a still clamps against the real per-asset `Asset.minDailyRate`/`maxDailyRate` (see masterplan "Locked decisions")
- Purpose: unblock the upcoming agent prototype so it can fetch experimental ML pricing before Phase 2 productionizes the real service
- Verification: lightweight `__main__` smoke-check block only (one prediction per category), consistent with `shap_review.py`/`category_metrics.py` — no new `tests/` files for this ml-experiments scratch code

**Day 3 (extension) — Phase 1d: real-time features (ml-experiments)** ✅ done
- `pricing_tables.py`: `CAPACITY_BINS`/`HEIGHT_BINS` + lead-time/utilization constants (see file comments and masterplan for rationale)
- `feature_schema.py`: `period_utilization`/`lead_time_days` in `NUMERIC_FEATURES`; new `spec_band()`
- `generate_synthetic_data.py`: sampling for both features, `firmness_premium()` rewired to live `period_utilization`, References docstring, sanity checks
- `train.py`: rerun only. `shap_review.py`: two new sweep checks, renumbered `[n/9]`
- `predict_price.py`: both features as **optional kwargs with fallback defaults** (keeps `pricing_client.py` working pre-Phase-1e)
- `demo_scenarios.py` (new): condition/duration scenario pairs, raw+clamped bar charts — see masterplan for why it's here and not Day 5
- Also: `booking_month` resolved, fuel price documented rejected, masterplan/spec updated to reflect all of the above, SHAP importance tuned (see masterplan)

**Day 3 (extension, cont.) — Phase 1e: production DB wiring** ✅ done (2026-08-10)
- First-ever read-only SQLAlchemy `AssetCategory`/`Asset`/`Booking` models (`app/models/`, `schema="primary_snapshot"` — see masterplan) + `app/repositories/pricing_repository.py` (`compute_period_utilization()`, `compute_lead_time_days()`, `resolve_effective_capacity()`). **Also added `app/models/booking_item.py`**, not in the original model list — see masterplan's Phase 1e change-log entry for why (`Booking` has no `asset_id`; the asset link is only via `BookingItem`).
- Shared read-resilience resolver implemented as `app/repositories/pricing_read_resilience.py`'s `resolve_pricing_schema()`: 3-tier fallback for every `primary_snapshot` read — retry on transient mid-recreate failure, degrade to reading `public` on sustained failure (mark the prediction degraded via `pricing_client.py`'s existing `model_version`/`explanation` fields, no new dataclass field), fail loud (`PricingSchemaUnavailable`) on cold start. Implemented via SQLAlchemy's `schema_translate_map` execution option, so `compute_period_utilization()` doesn't need a second set of `public`-targeting models. Reused as-is by Phase 2a's guardrail-bound `Asset` read when that lands.
- Threaded `db`/`start_date`/`end_date` through the call chain that actually exists today: `RecommendationService` (`app/services/recommendations.py`) → `PredictPriceAdapter` (`app/pipelines/predict_price_adapter.py`) → `predict_price_for_asset()` (`app/services/pricing_client.py`). **`app/api/recommendations.py` was not touched** — no route calls `RecommendationService` yet (it's exercised only by tests today), so there was nothing to thread there; `RecommendationService.__init__` now accepts an optional `db: Session | None` ready for when that route lands. `db=None` (today's only real caller) is the graceful-fallback path, unchanged from pre-Phase-1e behavior — not a placeholder for a failure case.
- Status filter, overlap rule, and `capacity` null fallback per masterplan/spec §5.2 — the capacity fallback moved into `pricing_client.py`'s `predict_price_for_asset()` (was a `0.0`-on-null bug in `PredictPriceAdapter.run()` before this).
- Tests: `tests/test_pricing_read_resilience.py`, `tests/test_pricing_repository.py`, `tests/test_pricing_client_phase1e.py`, `tests/test_pricing_phase1e_wiring.py` (20 new, all against a mocked session per spec §6 — no real fault-injection setup available).

**Day 4 — Phase 2a: productionize pricing service** ✅ done (2026-08-11)
- Built `app/services/pricing/` package: `model.py`, `train.py`, `feature_schema.py`, `pricing_tables.py`, `artifacts/` — ported Phase 1d's `feature_schema.py`/`pricing_tables.py` unchanged, and **relocated** (didn't rebuild) Phase 1e's `pricing_repository.py`/`pricing_read_resilience.py` into this package as `repository.py`/`read_resilience.py` (`app/repositories/` is now empty).
- **Fixed the category-name mismatch** (found 2026-08-11 during Phase 2 prep, confirmed live against `heavy_rental`): `AssetCategory.name` (`Excavator`, `Scissors Lift`, `Boom Lift`, `Fork Lift`) never matched `feature_schema.CATEGORIES` (`excavator`, `scissor lift`, `boom lift`, `forklift`) — `compute_period_utilization()`'s join had silently always fallen back to the static per-category constant, no error, no degraded flag. New `category_mapping.py` module (`DB_NAME_TO_FEATURE_NAME` + reverse), applied inside `repository.py`'s `compute_period_utilization()`. Two new CI-safe regression tests assert on the compiled SQL's bound category literal — no live DB needed, closing the gap where a fully-mocked `session.execute` couldn't have caught this. Re-verified live post-fix: `period_utilization` now returns real, varied fractions (e.g. 0.75/0.25/0.0 across test windows), not the static constant.
- Guardrail clamping against `Asset.minDailyRate` / `maxDailyRate`: **implemented** — `min_daily_rate`/`max_daily_rate` are required parameters on the new `predict_price(...)`, clamped against directly, no static-table fallback anywhere in this package.
- Found and closed one more gap while removing that static table: an unrecognized `category` used to fail via an *incidental* `KeyError` from the guardrail-table lookup; with that lookup gone, a bad category would have silently one-hot-encoded to an all-zero row and predicted from garbage with zero error. Added an explicit `ValueError` check.
- `predict_price(...)` **implemented** as an in-process function (`app/services/pricing/model.py`) — not yet called from `app.pipelines`; that wiring is Day 5's task below, unchanged in scope.
- 24 new tests (`test_pricing_feature_schema.py`, `test_pricing_model.py`, +2 in `test_pricing_repository.py`); full suite 144 passing (was 120). Live-verified against all 27 real `heavy_rental` assets. Full detail: `openspec/specs/dynamic-pricing/spec.md`/`design.md` change control 2.5.0.
- **Finding from the live verification, not a Phase 2a defect** — see the new Phase 2d entry below.

**Day 5 — Phase 2c: internal pricing quote API** ✅ done (2026-08-11) (new 2026-08-11, not part of the original 5-day scope; **resequenced 2026-08-11 to run before Phase 2b** — its only dependency is Phase 2a, already done; see masterplan "Phase 2b/2c sequencing and lean 2b scope")
- Dependency satisfied: Phase 2a's guardrail clamping + category-name mapping fix are done — this endpoint's contract already promises Spring Boot real per-asset guardrail bounds; building it against `ml-experiments/predict_price.py`'s static stand-in would ship the wrong shape.
- Added `app/api/internal_pricing.py`: `POST /internal/v1/pricing/quote`, reusing `app/services/pricing/model.py`'s `predict_price(...)` — no second prediction path. Registered directly on the app in `app/main.py`, not via `api_router`, so it's outside the public `/api/v1` prefix. New `app/schemas/pricing.py` (request/response models) and `app/services/pricing/repository.py::get_asset_for_pricing()` (server-side asset resolution by `asset_id`, through the same tiered read-resilience resolver as every other pricing read).
- Request/response shape, per-item independent `degraded`/`model_version`, dropped `/estimate`, fixed `deposit_rate`, excluded `raw_price`: see masterplan's "Internal pricing quote API" and `openspec/specs/dynamic-pricing/design.md` "Internal quote API" for the full contract — not duplicated here.
- Unit/integration tests per spec Verification (multi-item shape, per-item guardrail bounds, per-item degraded independence, unresolvable `asset_id` handling, route-inventory check). **Done**: `tests/test_internal_pricing_api.py`, 5 new tests, 149 total passing.
- **Does not touch `pricing_client.py`/`predict_price_adapter.py`/`recommendations.py`** — those are Phase 2b's exclusive surface, which is why this ran first without blocking or being blocked by it.
- Implementation note: `asset_id` is `int` in the request/response schemas, matching the real `Asset.id` primary key — a deliberate resolution of design.md's illustrative example (which used string codes).

**Day 5 (cont.) — Phase 2b (lean): pipeline wiring only** (scope trimmed and resequenced after Phase 2c, 2026-08-11 — see masterplan "Phase 2b/2c sequencing and lean 2b scope")
- Wire the pricing function into the agentic pipeline → return the prediction in the recommendation response (`item.pricing.daily_rate`). Concretely: swap `pricing_client.py`'s `_ensure_loaded()`/`predict_price_for_asset()` to call `app.services.pricing.model.predict_price(...)` instead of the `ml-experiments` prototype (Phase 2a built and verified the target; this swap itself hasn't happened yet). No DB write: Spring Boot persists `RecommendationItem.mlPredictedPrice` on its side (locked 2026-08-11 — see masterplan)
- Pipeline-integration tests only — does `pricing_client.py` call the new function, does `item.pricing.daily_rate` populate correctly on the response. **Not** a re-test of guardrail clamping/feature-schema transforms/prediction shape — already covered by Phase 2a's 24 tests (`test_pricing_feature_schema.py`, `test_pricing_model.py`).
- ~~Manual "retrain now" endpoint~~ — **moved to subtask 5** (demo prep, below): it's a demo safety net with no pipeline dependency, doesn't need to gate this swap.
- ~~README, final polish, rehearse demo~~ — **stays subtask 5**, not re-merged into this subtask's scope (see Jira table).

**Day 6+ (future, not part of the original 5-day build) — Phase 2d: guardrail/duration-discount scale calibration** (new 2026-08-11, found during Phase 2a's live verification)
- **Finding**: live-testing `predict_price(...)` against all 27 real `heavy_rental` assets showed the raw model output falls **below** `Asset.minDailyRate` for nearly every asset at a typical rental duration — 25 of 27 assets clamped upward at `duration_days=7`. At `duration_days=1` (no duration discount applied) the picture is very different and much more mixed: 18 of 27 clamped, in both directions, not just up. This points to a scale mismatch, not randomness: the model was trained with a non-linear duration discount (price drops for longer rentals, by design — down to ~0.35x at long durations, see `ml-experiments/pricing_tables.py`'s `DISCOUNT_FLOOR`/`DISCOUNT_RATE`), but the real seeded `minDailyRate`/`maxDailyRate` bounds look like they represent a near-1-day rate floor/ceiling, not a duration-adjusted range. For any multi-day rental — i.e. most real bookings — the guardrail clamp is currently overriding most of the model's duration signal rather than lightly bounding it.
- **Not a Phase 2a defect.** Guardrail clamping does exactly what spec.md requires: clamp raw output to `[minDailyRate, maxDailyRate]`. This is a data/model calibration question the spec never anticipated, surfaced only once real per-asset bounds (this reseed) and real per-asset clamping (Phase 2a) both existed to reveal it — neither alone would have shown it (static category-table guardrails were too coarse; the old 8-asset fixture too thin to see the pattern clearly).
- **Why it matters**: it undercuts the "meaningful, differentiated pricing" goal the seed-data richening (`openspec/specs/domain-seed-data/spec.md`) was specifically for — if the guardrail clamp dominates at realistic durations, two assets with very different `condition`/`capacity`/`period_utilization` can still converge on the same clamped price (their shared `minDailyRate`), which is close to what richer seed data was meant to avoid.
- **Options to evaluate (not yet decided — needs a product/pricing-owner call, not a unilateral code fix)**:
  1. Retrain against a synthetic dataset re-anchored to reflect that `minDailyRate`/`maxDailyRate` may represent an undiscounted, near-1-day reference rate rather than a duration-agnostic bound — i.e. teach the model to predict closer to that reference for short/typical durations, discounting further only where the guardrail band has room.
  2. Confirm with whoever owns the real `Asset.minDailyRate`/`maxDailyRate` values (Spring Boot / pricing policy) what they're actually meant to represent — a 1-day rate, or a bound valid at any duration — before choosing a fix in either direction.
  3. Reconsider whether duration-discounting should apply at all once a prediction is already within the guardrail band, vs. always applying the full discount curve and letting clamping correct it (today's behavior).
- **Verification**: re-run the Phase 2a live check (all 27 assets, a spread of durations — 1, 7, 14, 30 days) after any calibration change; target a materially lower clamp rate at realistic demo durations (7–14 days), not just at `duration_days=1`.

---

## Jira subtasks / branches

| # | Status | Jira subtask | Branch | Covers | Day |
|---|---|---|---|---|---|
| 1 | ☑ | Phase 1 — synthetic data generation | `feature/ml-1-synthetic-data` | Scaffold `ml-experiments/` + full `generate_synthetic_data.py` | 1 |
| 2 | ☑ | Phase 1 — feature engineering, training & SHAP review | `feature/ml-2-train-and-shap` | Feature engineering, baseline training, SHAP review, finalize `feature_schema.py`, retrain final model, save artifacts | 2–3 |
| 2b | ☑ | Phase 1c — prototype `predict_price()` (ml-experiments) | `feature/ml-2b-predict-price-prototype` | `ml-experiments/predict_price.py`: in-process prototype + guardrail clamping (static per-category bounds), for the upcoming agent prototype | 3 (ext.) |
| 2c | ☑ | Phase 1d — real-time features (ml-experiments) | `HR-75-ml-2-c-period-utilization-lead-time` | See Day 3 (extension) above; also `demo_scenarios.py` (nominally subtask 5 scope, scaffolded early) | 3 (ext., cont.) |
| 2d | ☑ | Phase 1e — production DB wiring for `period_utilization` | `HR-87-ml-2-d-production-db-wiring-for-period-utilization` | First-ever read-only `AssetCategory`/`Asset`/`Booking`/`BookingItem` models (`app/models/`), `app/repositories/pricing_repository.py` + its shared 3-tier read-resilience resolver (`app/repositories/pricing_read_resilience.py`, spec §5.3.1), wired through `pricing_client.py`/`predict_price_adapter.py`/`recommendations.py` (no live API route calls this pipeline yet, so `app/api/recommendations.py` itself wasn't touched) | 3 (ext., cont.) |
| 3 | ☑ | Phase 2 — productionize pricing service | `feature/ml-3-pricing-service` | `app/services/pricing/` package, real per-asset guardrail clamping, in-process `predict_price()` function, **category-name mismatch fix** (2026-08-11). **Done (2026-08-11)** — 24 new tests, 144 total passing, live-verified against all 27 real assets. | 4 |
| 4 | ☐ | Phase 2 — pipeline integration & tests (lean, 2026-08-11) | `feature/ml-4-integration-tests` | Wire into agentic pipeline → return prediction in response (no DB write — Spring Boot persists `mlPredictedPrice`, **locked**); pipeline-integration tests only (guardrail clamping/feature-schema tests already done in subtask 3). Manual "retrain now" endpoint moved to subtask 5. **Now sequenced after subtask 6**, not before — see the Build order note below. | 5 (cont.) |
| 5 | ☐ | Demo prep & docs | `feature/ml-5-demo-prep` | README, rehearse demo, final polish, manual "retrain now" endpoint (moved from subtask 4, 2026-08-11 — demo safety net, no pipeline dependency). **Live demo script itself already built under subtask 2c.** Rehearsal should wait until subtask 7 (Phase 2d) resolves the guardrail/duration-discount clamp-dominance finding, or use short/`duration_days=1` scenarios meanwhile, to avoid hitting that finding live. | 5 (PM) |
| 6 | ☑ | Phase 2c — internal pricing quote API (new 2026-08-11) | `feature/ml-6-internal-pricing-api` | `app/api/internal_pricing.py` (`POST /internal/v1/pricing/quote`), `app/schemas/pricing.py`, `repository.py::get_asset_for_pricing()`, reusing `app/services/pricing/model.py`. **Done (2026-08-11)** — 5 new tests, 149 total passing. Resequenced 2026-08-11 to build before subtask 4 — doesn't touch `pricing_client.py`, so it had no dependency on subtask 4 either. See masterplan/spec/design "Internal quote API"/"Internal pricing quote API". | 5 |
| 7 | ☐ | Phase 2d — guardrail/duration-discount scale calibration (new 2026-08-11) | TBD | Resolve the scale mismatch found during subtask 3's live verification (see Day 6+ section above). Needs a product/pricing-owner decision before implementation, not just a code change. Branch not yet created. | 6+ (future) |

**Build order (updated 2026-08-11):** 3 (done) → 6 (done) → 4 (lean, next) → 5 → 7 (whenever a product/pricing-owner decision unblocks it). Subtask numbers reflect the order each was added to this plan, not build order — 6 was scoped after 4 but built first, and 4's scope shrank the same day. Full rationale: masterplan "Phase 2b/2c sequencing and lean 2b scope."

PR/review notes:
- Subtasks 1–2, 2b, 2c (`ml-experiments/`, scratch code): lighter-weight review, merge fast.
- Subtasks 2d, 3–4, 6 (`app/`, production code): full review — this is what the agentic pipeline, Spring Boot, and teammates depend on. 2d specifically touches the live recommendation pipeline ahead of the rest of Phase 2, so review it with the same weight as 3–4/6, not as scratch. Subtask 6 also gets external scrutiny since Spring Boot integrates directly against its contract. Subtask 7 (Phase 2d) needs a decision first — don't start implementation before that's made.

---

## Open items carried from masterplan

- [ ] Real retrain interval (monthly vs. quarterly) — not needed for this 5-day build, deferred to Phase 3.
- [ ] Full APScheduler scheduled retrain — explicitly out of scope for this build; manual "retrain now" endpoint is the stand-in.
- [x] ~~**New (2026-08-11):** category-name mismatch fix — folded into Phase 2a (`feature/ml-3-pricing-service`), not a separate subtask.~~ — **done (2026-08-11, same day)**: `category_mapping.py` added, applied in `repository.py`. See spec/design change control 2.5.0.
- [x] ~~**New (2026-08-11):** richer Spring Boot seed data (`openspec/specs/domain-seed-data/spec.md`) — external dependency, tracked here for coordination but not a Haystack branch/subtask.~~ — **done (2026-08-11, same day)**: Spring Boot executed the reseed (8→27 assets, 20→90 bookings); Haystack independently verified against `heavy_rental`. See `openspec/specs/domain-seed-data/spec.md` "State after reseed". Category-name mismatch fix (still open, above) is the only remaining item before `period_utilization` reflects this data through the real code path.
- [x] ~~`booking_month`/seasonality as a feature — Phase 1b found a mild, not-fully-clean per-month error pattern (January worst); lean is against adding it, but not locked. Decide during Phase 2, before finalizing the productionized `feature_schema.py`.~~ — **resolved in Phase 1d: not added** (`period_utilization` already captures realized seasonality). See masterplan.
- [x] ~~Phase 1c's guardrail bounds (`ml-experiments/predict_price.py`, static per-category `pricing_tables.CATEGORY_BASE_RATE`) are a stand-in only — Phase 2a must still clamp against the real per-asset `Asset.minDailyRate`/`maxDailyRate`, not reuse this prototype's bound source.~~ — **done (2026-08-11)**: `min_daily_rate`/`max_daily_rate` are required parameters on the production `predict_price(...)`, no static fallback in `app/services/pricing/`.
- [x] ~~**New (2026-08-11):** internal pricing quote API (`feature/ml-6-internal-pricing-api`, `POST /internal/v1/pricing/quote`) — hard dependency (subtask 3) now satisfied; resequenced 2026-08-11 to build before subtask 4 (lean Phase 2b), not after.~~ — **done (2026-08-11)**: `app/api/internal_pricing.py`, `app/schemas/pricing.py`, `repository.py::get_asset_for_pricing()`; 5 new tests, 149 total passing. See spec/design change control 2.6.0.
- [ ] **New (2026-08-11):** sanity-check Spring Boot's real computed `distance_km` values against the synthetic training distribution before trusting prediction quality at scale — the model was trained on a sampled proxy, not real postal-code distances. Not scheduled to a specific subtask yet.
- [ ] **New (2026-08-11):** recommend-price vs. quote-price drift (both draw on live `period_utilization`/`lead_time_days`) is intentionally left unreconciled for now — revisit if it causes real confusion in the demo/UI.
- [ ] **New (2026-08-11):** guardrail/duration-discount scale calibration — see the new "Phase 2d" section above (Day 6+) and Jira subtask 7. Needs a product/pricing-owner decision (what `minDailyRate`/`maxDailyRate` are meant to represent) before any code change.
- [x] ~~**Before implementing Phase 1e's `period_utilization` aggregate query**: confirm `BookingStatus.CONFIRMED` actually exists (only `PENDING`/`CANCELLED` are ever named in this repo) and confirm exact `Asset`/`Booking` table/column names and casing against the real Spring Boot schema — the masterplan's field list is diagram-sourced, not verified.~~ — **resolved (2026-08-09)**: see masterplan for full detail (snake_case columns, real `BookingStatus` lifecycle, status filter, overlap rule). **Correction, same day**: cross-checking `SPEC-spring-entity-repository.md` found `BookingStatus` includes `CANCELLED` (real, reachable — cancellation happens); an earlier "happy-path-only" claim in this resolution was wrong. Filter membership itself didn't change — see masterplan.
- [x] ~~Confirm `Asset.capacity` is reliably populated before relying on it as a spec-band bucketing key.~~ — **resolved (2026-08-09)**: null for every non-forklift seed row; permanent per-category fallback added (masterplan, spec §5.2).
- [x] ~~Confirm which Postgres schema pricing's SQLAlchemy models should read from.~~ — **resolved (2026-08-09)**: `primary_snapshot`, not `public` (masterplan, spec §5.3).
- [x] ~~`Asset.condition` is nullable (`SPEC-spring-entity-repository.md` §5.3) and `feature_schema.encode_condition()` currently raises `ValueError` on a null value — needs a fallback default, same category of fix as `capacity`. Not yet designed; pending a decision on the default value.~~ — **resolved (2026-08-09)**: falls back to `"GOOD"`, not NaN (masterplan, spec §5.2/§8).

---

## Change log (of this execution plan, not the feature)

| Date | Note |
|------|------|
| 2026-08-04 | Initial execution plan split out from `dynamic-pricing-masterplan.md`. Consolidated to 5 Jira subtasks/branches, reduced from an earlier 13-subtask draft to cut PR frequency, especially during Phase 1. |
| 2026-08-04 | Added `distance_km` to the Day 1 generator task and Day 2–3 feature-engineering list, per the new locked `distance_km` decision in the masterplan. |
| 2026-08-04 | Phase 1b complete: subtasks 1–2 checked off. Added `platform_height` to the Day 2–3 feature-engineering list — discovered mid-review via a per-category MAE/R² breakdown showing boom lift/scissor lift fitting far worse than forklift/excavator; see masterplan for the decision. Next: write `SPEC-dynamic-pricing.md` (+ `SPEC-domain-seed-data.md`) per the masterplan's phase order, before starting Phase 2. |
| 2026-08-04 | `SPEC-dynamic-pricing.md` written. Added `booking_month`/seasonality as an open item carried into Phase 2 (Day 4 task + Open items list) — a per-`booking_month` MAE/R² check found a mild pattern worth a deliberate decision, not locked either way. |
| 2026-08-05 | Added Phase 1c: prototype `predict_price()` in `ml-experiments/` (new "Day 3 (extension)" task, subtask 2b) — a lightweight, DB-free version so the upcoming agent prototype can fetch experimental ML pricing before Phase 2a lands. Guardrail bounds are a static per-category stand-in, not the real per-asset clamp Phase 2a still implements. |
| 2026-08-07 | Added Phase 1d (real-time features, subtask 2c, branch `HR-75-ml-2-c-period-utilization-lead-time`) and Phase 1e (production DB wiring for `period_utilization`, subtask 2d, separate branch TBD) as new "Day 3 (extension, cont.)" tasks. Resolved the `booking_month` open item (not added). Added a new open item: confirm `BookingStatus.CONFIRMED` and exact Asset/Booking column casing before Phase 1e. Updated Day 4's task list to note `app/services/pricing/feature_schema.py` picks up the new features automatically via the Phase 1d port, and that Phase 2 relocates rather than rebuilds Phase 1e's repository query. |
| 2026-08-07 | Corrected Phase 1d's `predict_price.py` task: the two new features are **optional kwargs with fallback defaults**, not required — required kwargs would break `pricing_client.py`'s existing call the moment Phase 1d merges, ahead of Phase 1e threading real values through. Added `demo_scenarios.py` (script-based live demo for a lecturer, since `predict_price(...)` is in-process only) to subtask 2c's scope, pulled forward from its nominal `feature/ml-5-demo-prep` slot since it's low-risk (placeholder data, no dependency on the rest of Phase 1d). Updated subtask 5's row to reflect that the script itself is already built by then — remaining Day 5 PM scope is rehearsal/polish only. |
| 2026-08-07 | Subtask 2c (Phase 1d) marked done — implemented and verified. Condensed Day 3/Day 4 task descriptions (rationale now lives only in masterplan, per this file's own "don't duplicate reasoning" convention); no scope changes. |
| 2026-08-09 | Resolved the schema/casing/`BookingStatus`/capacity/DB-topology open items by confirming directly against `postgres-haystack`/`postgres-primary` — unblocks Phase 1e (see Open items). Updated Phase 1e's Day 3 (extension, cont.) task description accordingly; no longer "blocked." Full detail in masterplan/spec, not duplicated here. |
| 2026-08-09 (correction, same day) | Cross-checked the above against a new `SPEC-spring-entity-repository.md` (authoritative Java entity/enum reference). Corrected the `BookingStatus` open item: `CANCELLED` is real, cancellation happens — not happy-path-only as recorded earlier the same day. No change to `period_utilization`'s filter membership. Added a new open item: `Asset.condition` is nullable and crashes `feature_schema.encode_condition()` today — needs a fallback default, not yet designed. |
| 2026-08-09 (later same day) | Resolved the `condition` open item: falls back to `"GOOD"`, not NaN (masterplan, spec §5.2/§8). |
| 2026-08-10 | Added the shared 3-tier read-resilience resolver (spec §5.3.1) to Phase 1e's scope — retry on transient `primary_snapshot` failures, degrade to `public` on sustained failures, fail loud on cold start. Updated Phase 1e's Day 3 (extension, cont.) description and subtask 2d's "Covers" cell; no schedule/branch change. |
| 2026-08-10 (later) | **Phase 1e implemented and verified** on `HR-87-ml-2-d-production-db-wiring-for-period-utilization` (117 tests pass, 20 new). Marked subtask 2d done. Found and fixed a real gap while implementing: the real schema has no `Booking.asset_id` (`SPEC-spring-entity-repository.md` §5.7/§7) — the asset link is only via `BookingItem.asset_id`/`booking_id`, not called out in §5.3's original model list. Added `app/models/booking_item.py` as a 4th read-only model; see masterplan's Phase 1e change-log entry for the full correction. Confirmed `app/api/recommendations.py` doesn't call `RecommendationService` yet (only tests do today), so threading stopped at the service/pipeline layer rather than inventing a new route — `RecommendationService.__init__`'s new `db` param is ready for when that route lands. Moved the `capacity` null fallback (§5.2) into `pricing_client.py`'s `predict_price_for_asset()`, fixing a pre-existing `capacity=0.0`-on-null bug in `PredictPriceAdapter.run()`. |
| 2026-08-10 (persistence decision) | **Agreed in principle, pending confirmation from the Spring Boot side**: Haystack does not persist `mlPredictedPrice`. Subtask 4 / Day 5 Phase 2b's "wire into pipeline → persist" retitled to "→ return in response" — the persistence task moves to the Spring Boot side. No branch/schedule change; Phase 1e's read-only DB access (subtask 2d) is unaffected and becomes the service's only DB access going forward. Full rationale: masterplan, "Persistence: Haystack does not write `mlPredictedPrice`." |
| 2026-08-11 (Phase 2 prep) | **Persistence decision locked** — no longer pending; Spring Boot confirmed. Updated the Day 5 task line, subtask 4's "Covers" cell, and this file's other references accordingly. **Found and scoped a real bug** during Phase 2 prep checks (live-queried `heavy_rental`): `AssetCategory.name` (DB: `Excavator`/`Scissors Lift`/`Boom Lift`/`Fork Lift`) never matches `feature_schema.CATEGORIES` (code: `excavator`/`scissor lift`/`boom lift`/`forklift`) — `compute_period_utilization()` has silently always fallen back to its static per-category constant instead of running its live-query path, undetected because the existing unit test mocks `session.execute` directly. Folded the fix into subtask 3 (Phase 2a), not a new subtask — full detail in `openspec/specs/dynamic-pricing/spec.md`/`design.md` change control 2.2.0. **Also decided**: richer Spring Boot seed data is now a Phase 2 prep dependency (new `openspec/specs/domain-seed-data/spec.md`), not deferred wholly to Phase 3 — today's 8-asset/20-booking fixture (2 per category, capacity null on 6 of 8, single stale 11-day booking window, half the bookings missing their `BookingItem` link, only 4 of 6 `BookingStatus` values ever exercised) is too thin to verify Phase 2's own acceptance scenarios meaningfully. Tracked as an external dependency in Open items, not a Haystack branch. |
| 2026-08-11 (seed data landed, same day) | **Spring Boot executed the reseed the same day it was specced.** 8→27 assets (capacity backfilled + populated on all new rows, all 4 `ConditionType` values per category), 20→90 bookings (all 6 `BookingStatus` values present, including previously-absent `PENDING_DEPOSIT`/`CANCELLED`), 0 orphaned bookings (the 10 previously-missing `BookingItem` links backfilled), booking window now 2026-06-22→2026-09-24. Actual band distribution deviated from `domain-seed-data/design.md`'s original 3/2/1 suggestion — Spring Boot instead put 4 assets in one "main" band per category and 1 in every other band, which the design doc's Change control now records as the better call, not a deviation to correct. Haystack independently re-verified all of this live (not just trusting the summary) and confirmed `period_utilization` would return varied, non-degenerate fractions (0.75/0.25/0.0 across three test windows on Excavator's main band) once the still-open category-mapping fix lands. Marked the seed-data open item above done. Full detail: `openspec/specs/domain-seed-data/spec.md` "Execution result"/"State after reseed", `openspec/specs/dynamic-pricing/spec.md` change control 2.3.0. `openspec/specs/domain-seed-data/` kept (not deleted) as the standing data-shape contract for future reseeds/regression checks. |
| 2026-08-11 (internal quote API scoped, later same day) | **New subtask added: internal pricing quote API.** Spring Boot needs a synchronous, service-to-service endpoint for checkout-time authoritative pricing. Added "Day 5 (extension) — Phase 2c" task and subtask 6 (`feature/ml-6-internal-pricing-api`, `POST /internal/v1/pricing/quote`) — **hard dependency on subtask 3** (real per-asset guardrail clamping + category-name mapping fix), can run alongside/after subtask 4. Added 3 new Open items (endpoint dependency, `distance_km` sanity-check follow-up, recommend-vs-quote price drift left unreconciled). Full rationale and contract: masterplan's "Internal pricing quote API", `openspec/specs/dynamic-pricing/spec.md` change control 2.4.0, `design.md` "Internal quote API". |
| 2026-08-11 (Phase 2a implemented, later same day) | **Subtask 3 (Phase 2a) done.** Built `app/services/pricing/` (`model.py`, `train.py`, `feature_schema.py`, `pricing_tables.py`, `category_mapping.py`, `repository.py`, `read_resilience.py`, `artifacts/`); fixed the category-name mismatch (`category_mapping.py`, applied in the relocated `repository.py`); real per-asset guardrail clamping implemented (`min_daily_rate`/`max_daily_rate` now required parameters, no static fallback); closed an unplanned validation gap found while removing the static guardrail table (unrecognized `category` would have silently produced an all-zero one-hot row with no error — now raises `ValueError`). 24 new tests, 144 total passing. `pricing_client.py`'s actual predict-routing is unchanged (still calls the `ml-experiments` prototype; only its imports were updated, mechanically required by the relocation) — the real swap stays subtask 4's job. Marked subtasks 3 and the two related Open items (category-mismatch fix, guardrail-bounds stand-in) done. Updated Day 5's task line to spell out what the pipeline swap concretely means now that the target exists. Full detail: `openspec/specs/dynamic-pricing/spec.md`/`design.md` change control 2.5.0. **Also added "Phase 2d" (Day 6+, new subtask 7)**: live-verifying `predict_price(...)` against all 27 real assets surfaced a guardrail/duration-discount scale mismatch — at `duration_days=7`, 25 of 27 assets clamped upward (raw output below `minDailyRate`); at `duration_days=1`, a much more mixed 18/27 in both directions. Not a Phase 2a defect (clamping does exactly what's specified) but a real finding that undercuts the "differentiated pricing" goal the seed-data reseed was for — needs a product/pricing-owner decision (what do `minDailyRate`/`maxDailyRate` represent — a 1-day rate or a duration-agnostic bound?) before any code change. New Open item added; branch not yet created. |
| 2026-08-11 (2b/2c resequenced, later still) | **Resequenced Phase 2c before Phase 2b, and trimmed Phase 2b to a lean pipeline-wiring-only scope.** Reason: 2c depends only on Phase 2a (done) — it never touches `pricing_client.py`/`predict_price_adapter.py`/`recommendations.py`, Phase 2b's exclusive surface — so the original Day 5 AM/ext. ordering was drafting order, not a real dependency. Reordered the Day 5 sections (Phase 2c now first), reworded subtask 4/5/6's "Covers"/"Day" cells, added a "Build order" note after the Jira table (3 → 6 → 4 → 5 → 7), and updated the internal-quote-API open item. Trimmed subtask 4: guardrail-clamping/feature-schema unit tests dropped (already covered by subtask 3's 24 tests), manual "retrain now" endpoint moved to subtask 5 (demo safety net, no pipeline dependency). Subtask 5 also now carries a note to sequence demo rehearsal after subtask 7 (Phase 2d) resolves the clamp-dominance finding, or to use short/`duration_days=1` scenarios in the meantime. Full decision + rationale: `docs/dynamic-pricing-masterplan.md`, new "Phase 2b/2c sequencing and lean 2b scope" subsection, same-day change log entry. |
| 2026-08-11 (Phase 2c implemented, later still) | **Subtask 6 (Phase 2c) done — built first, ahead of subtask 4, per the resequencing above.** Added `app/api/internal_pricing.py` (`POST /internal/v1/pricing/quote`, registered directly on the app in `app/main.py`, not via `api_router` — outside the public `/api/v1` prefix), `app/schemas/pricing.py` (request/response models), and `app/services/pricing/repository.py::get_asset_for_pricing()` (+ `AssetPricingRow`) to resolve category/condition/capacity/platform_height/guardrail bounds server-side from `asset_id` alone, through the same tiered `read_resilience` resolver as every other pricing read. Reuses `predict_price(...)` unchanged — no second prediction path. One implementation resolution beyond the illustrative contract: `asset_id` is `int` (the real `Asset.id` PK type), not the string-code form design.md's JSON example showed. Each item resolves its schema twice (once for the asset read, once inside `predict_price(...)` itself for `period_utilization`) — both real tiered resolutions against the same resolver, combined via OR into the item's `degraded` flag; a cold-start `PricingSchemaUnavailable` is deliberately not caught per-item (systemic, not asset-specific) and propagates to the global 500 handler instead. 5 new tests (`tests/test_internal_pricing_api.py`): multi-item shape, per-item guardrail bounds from real `Asset` rows, per-item `degraded` independence, unresolvable `asset_id` per-item error, route-inventory (via `app.openapi()["paths"]`, since this FastAPI version wraps `include_router()` results in an internal `_IncludedRouter` with no stable public `.path`). 149 total tests passing (was 144). Marked subtask 6 done and the internal-quote-API open item done. Full detail: `openspec/specs/dynamic-pricing/spec.md`/`design.md` change control 2.6.0. |
