# Standalone REASONS canvases

Capability canvases live at `openspec/specs/<cap>/design.md`.

Add a file here only when a canvas is **not** tied to a live capability spec (spike, cross-repo sketch). Index: [`../README.md`](../README.md).
